{
  "name": "emr-monorepo",
  "version": "1.0.0",
  "description": "EMR Monorepo with PatientPortal frontend",
  "private": true,
  "workspaces": [
    "PatientPortal"
  ],
  "scripts": {
    "build": "npm run build --workspace=PatientPortal",
    "install": "npm install --workspace=PatientPortal",
    "dev": "npm run dev --workspace=PatientPortal"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
